---
import EyebrowPill from './ui/EyebrowPill.astro'

const benefits = [
  {
    title: 'Profile Isolation',
    description: 'Keep each account context separate so client, personal, and team sessions do not leak into each other.',
    icon: '🔒',
  },
  {
    title: 'Lightweight Sync',
    description: 'Profiles, accounts, links, hidden networks, and key preferences follow your account while live login cookies remain local.',
    icon: '☁️',
  },
  {
    title: 'Built-In Planning',
    description: 'Use custom links today, with a CRM and Kanban workspace for publishing tasks currently in development.',
    icon: '📅',
  },
  {
    title: 'Desktop, Mobile, Extension',
    description: 'Run the same workflow across web, browser extensions, desktop, and Android with one shared codebase.',
    icon: '📱',
  },
]
---

<section id="benefits" class="py-16 sm:py-24 bg-card">
  <div class="max-w-6xl mx-auto px-4">
    <div class="text-center mb-12 sm:mb-16" data-reveal>
      <EyebrowPill class="animate-fade-up">Benefits</EyebrowPill>
      <h2
        class="animate-fade-up delay-100 text-3xl sm:text-4xl font-bold text-foreground"
        style="font-family: var(--font-instrument-sans);"
      >
        Why Choose CommunityGlows
      </h2>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-4" data-reveal>
      {benefits.map((benefit, index) => (
        <div
          class={`animate-fade-up delay-${(index + 1) * 100} group flex gap-4 sm:gap-6 items-center p-6 rounded-2xl bg-background border border-border transition-all duration-300 site-interactive-card`}
        >
          <div class="shrink-0 bg-muted rounded-lg p-3 text-3xl">
            {benefit.icon}
          </div>
          <div>
            <h3 class="text-xl sm:text-2xl font-semibold text-foreground mb-2">{benefit.title}</h3>
            <p class="text-base text-muted-foreground leading-relaxed">{benefit.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
